# Godot version

Use the Godot API version specified by `config/features` in the `project.godot` file.

# C# Code

Use the `Godot.NET.Sdk` version specified in the `.csproj` file.

Prefer the C# Godot API over GDScript idioms.
